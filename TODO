ENSURE True/False can be passed to vector new

keep a PyObject for dimensions, width, and height on the vector that can be read using https://docs.python.org/2/extending/newtypes.html#generic-attribute-management

prevent strings as argument to vector

python-visible docstrings for all modules & methods

standardized docstrings for all functions

de-init - need to decref self?

remove toTuple, implement iterator protocol

make an iter to an obj, then delete it - what does iter.next do
